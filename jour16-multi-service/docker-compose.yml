version: '3'

services:

  web:
    image: nginx
    ports:
      - "8082:80"
    volumes:
      - .:/usr/share/nginx/html

  database:
    image: mysql:5.7
    environment:
      MYSQL_ROOT_PASSWORD: root123
